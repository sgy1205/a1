<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.smxy.forum.mapper.MenuMapper">

    <select id="queryPermissionByUserId" resultType="string">
        select
            t1.perms
        from
            menu t1
        left join
            role_menu t2
        on
            t1.menu_id = t2.menu_id
        left join
            role t3
        on
            t2.role_id = t3.role_id
        left join
            user_role t4
        on
            t4.role_id = t3.role_id
        where
            t4.user_id = #{userId}
        and
            t3.status = 0
        and
            t1.status = 0
    </select>

    <select id="getAllermission" resultType="string">
        select perms from menu where status = 0
    </select>

    <select id="getNumberOfRoleMenu" resultType="integer">
        SELECT
            COUNT(DISTINCT role_id) AS role_count
        FROM
            role_menu
        WHERE
            menu_id =#{menuId};
    </select>
</mapper>
