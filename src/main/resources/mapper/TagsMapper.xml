<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.smxy.forum.mapper.TagsMapper">

    <select id="getTagsListVoByPostId" resultType="tagsListVo">
        select
            t1.tags_id,
            t1.tags_name
        from
            tags t1
        left join
            tags_post t2
        on
            t1.tags_id = t2.tags_id
        where
            t2.post_id = #{postId}
    </select>

</mapper>
